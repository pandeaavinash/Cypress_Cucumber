name: Cypress Tests

on: push

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # Install npm dependencies, cache them correctly
      # and run all Cypress tests
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          command: npm run run-test
         command: npm run generate-report
      
      - name: Upload report
        uses:actions/upload-artifact@v2
        with:
        name: cypress-cucumber-report
        path: home/runner/work/Cypress_Cucumber/Cypress_Cucumber/cypress/reports/multiple-cucumber-html-report-html/
